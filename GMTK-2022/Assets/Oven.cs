using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Oven : MonoBehaviour
{
    [SerializeField]
    GameObject pizza;
    // Start is called before the first frame update
    void Start()
    {
        // pull in pizza rating, modify based on correctness of appliance use.
        // When player interacts with oven, have it start a timer
        // Once timer complete, player can pull from oven
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
